version: 2
jobs:
  one:
    docker:
      - image: circleci/node:4.8.2-jessie
    steps:
      - checkout
      - run: 
        command: echo "hello world"
  two:
    steps:
      - checkout
      - run:
        command: echo "hello world1"

workflows:
  test:
    jobs:
      - one
      - two

